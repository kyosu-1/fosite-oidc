version: '3'

tasks:
  migrate:
    desc: Run database migrations
    cmds:
      - migrate -path migrations -database ${DATABASE_URL} up
    vars:
      DATABASE_URL: "postgres://postgres:password@localhost:5432/oidc?sslmode=disable"


